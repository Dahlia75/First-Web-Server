<!DOCTYPE html>
  <html lang="en">
    <head>
      <%- include('partials/_header') %>
      <style>
        form {
          display: inline;
        }
      </style>
    </head>
    <body>
      <ul>
        <% for (var url in urls) { %>
          <li style="font-size:20px; padding:50px;"> <%= url %> : <%= urls[url] %>
            <a href="/urls/<%= url %>">Edit</a>
            <form method="POST" action="/urls/<%= url %>/delete">
              <button type="submit">Delete</button>
              <br/>
            </form>
          </li>
        <% } %>
      </ul>
    </body>
  </html>